en:
  endpoint:
    errors:
      must_be_hash: must be a hash
      must_contain_code: must contain a code
      must_contain_valid_code: must contain a valid HTTP status code
      headers_must_be_hash: headers must be a hash
      headers_only_string_keys_values: headers must only contain string keys and values
      body_must_be_string: body must be a string
      valid_uri_path: must be a valid URI path
      unique_verb_path: the combination of verb and path must be unique
      endpoint_not_fount: endpoint not found
      endpoint_missing_parameters: missing parameter error
      missing_parameters: "Missing required parameters: %{keys}"
